describe("Dummy test", () => {
  it("should have a valid randomValue", () => {
    const randomValue = "Horray ! Test is setup";
    expect(randomValue).toBe("Horray ! Test is setup");
  });
  it("should have a valid randomValue", () => {
    const randomValue = "Horray ! Test is setup";
    expect(randomValue).toBe("Horray ! Test is setup");
  });
});
